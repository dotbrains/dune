{
	"default_files": 25,
	"directories": {
		"test": {
			"limit": 80,
			"reason": "Tests are flat by behavior surface so individual files can run directly with bun test test/name.test.tsx."
		},
		"src/themes": {
			"limit": 35,
			"reason": "Each theme is a small independent palette registered from one directory."
		}
	},
	"exclude": ["^bun\\.lock$", "^screenshot\\.png$"]
}
