{
	"default_files": 25,
	"directories": {
		"test": {
			"limit": 80,
			"reason": "Tests are flat by behavior surface so individual files can run directly with bun test test/name.test.tsx."
		},
		"src/themes": {
			"limit": 35,
			"reason": "Themes stay discoverable as small palette, family, builder, and registry modules in one directory."
		}
	},
	"exclude": ["^bun\\.lock$"]
}
